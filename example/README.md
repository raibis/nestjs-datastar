# Nest Datastar Examples

## Setup

From the root of the project, run:

```bash
npm install pug
npm run build
```

Then, inside the `example` folder, run:

```bash
npm install
npm run start:debug
```
